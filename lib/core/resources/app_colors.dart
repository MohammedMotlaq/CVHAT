import 'package:flutter/material.dart';

class AppColors {
  // Background Colors.
  static const Color bgWhite = Colors.white;
  static const Color bgBlack = Colors.black;

  // App Colors.
  static const Color primary = Color.fromRGBO(0, 23, 31, 1.0);
  static const Color secondary = Color.fromRGBO(0, 52, 89, 1.0);
  static const Color secondary_2 = Color.fromRGBO(0, 126, 167, 1.0);
  static const Color secondary_3 = Color.fromRGBO(0, 166, 251, 1.0);

  // Text Colors.
  static const Color textWhite = Colors.white;
  static const Color textBlack = Colors.black;
  static const Color textPrimary = Color.fromRGBO(0, 23, 31, 1.0);
  static const Color textSecondary = Color.fromRGBO(0, 52, 89, 1.0);
  static const Color  textAccent = Color.fromRGBO(0, 166, 251, 1.0);
}
